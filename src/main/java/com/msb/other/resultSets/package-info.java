package com.msb.other.resultSets;

/**
 *
 * 1、测试resultSets
 *
 * 在测试resultSets时，resultSets是与resultSet搭配使用，
 *
 * 而使用resultSet之后，column和foreignColumn中配置的列数，必须相等，
 *
 * ⚠️所以resultSets、resultSet、column、foreignColumn是搭配在一起使用的！
 *
 * resultSets定义ResultSet名称
 *
 * resultSet定义引用的ResultSet的名称
 *
 * column定义当前表字段名，foreignColumn定义当前表字段名所关联的表的字段名（注意，是关联的表的字段名）
 *
 */
